{
  "name": "searchable-input",
  "version": "0.0.0",
  "main": "dist/index.js",
  "repository": "git@github.com:khirayama/searchable-input.git",
  "author": "khirayama",
  "license": "MIT",
  "engines": {
    "node": ">=6.8 <7",
    "npm": ">=3.10 <4"
  },
  "browserify": {
    "transform": [
      "coffeeify"
    ]
  },
  "scripts": {
    "pre-build": "mkdir -p ./dist",
    "build": "npm run build:scripts && npm run build:styles",
    "build:scripts": "npm run pre-build && $(npm bin)/browserify --extension='.coffee' ./src/scripts/index.coffee > ./dist/searchable-input.js",
    "build:styles": "npm run pre-build && $(npm bin)/node-sass ./src/styles/index.scss ./dist/searchable-input.css",
    "test": "$(npm bin)/mocha --recursive --compilers coffee:coffee-script/register ./test",
    "watch:scripts": "npm run pre-build && $(npm bin)/watchify ./src/scripts/index.coffee -v -o ./dist/searchable-input.js",
    "watch:styles": "npm run pre-build && $(npm bin)/node-sass -w ./src/styles/index.scss ./dist/searchable-input.css"
  },
  "dependencies": {
    "browserify": "^13.1.1",
    "coffee-script": "^1.12.2",
    "coffeeify": "^2.1.0",
    "gulp": "^3.9.1",
    "jsdom": "^9.9.1",
    "lodash": "^4.17.3",
    "mocha": "^3.2.0",
    "node-sass": "^4.1.1",
    "power-assert": "^1.4.2",
    "sinon": "^1.17.6",
    "watchify": "^3.8.0"
  }
}
